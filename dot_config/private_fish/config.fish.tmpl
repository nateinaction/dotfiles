# Generic utilities
{{- if stat (printf "%s/.local/bin" .chezmoi.homeDir) }}
set -gx PATH $PATH "$HOME/.local/bin"
{{- end }}

if status is-interactive
    alias ll="ls -lah"
{{- if .work }}

    # ai
    alias gemini='GOOGLE_GENAI_USE_VERTEXAI=true GOOGLE_CLOUD_PROJECT=wp-engine-ai GOOGLE_CLOUD_LOCATION=us-central1 gemini'
    alias claude='CLAUDE_CODE_USE_VERTEX=1 CLOUD_ML_REGION=us-east5 ANTHROPIC_VERTEX_PROJECT_ID=wp-engine-ai claude'
{{- end }}
end
